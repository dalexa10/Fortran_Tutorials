program hello
    ! Comment like this
    print *, 'This is Fortran'
end program hello

!Compile the code:
!   - Verify gfortan --version (if not, install it)
!   - Compile this gfortran file_name.f90 -o output_compiled_file